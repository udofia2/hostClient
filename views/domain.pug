<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Domain Search
    body
        h1 Search for domain availability
        form(action="", method="post")
            input(type="text", name="name")
            button(type="submit") Search
            